"""
The blind A/B listening test -- the only instrument that measures naturalness.

    listening_test.py     build a self-contained blind A/B HTML page
    listening_analyse.py  score the returned results (Wilson CI, exact binomial,
                          per-listener rates, catch trials, side-bias)

Fewer than three listeners is a pilot, not a finding.
"""
