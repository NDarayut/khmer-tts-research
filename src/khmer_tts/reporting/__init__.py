"""
Turning scores into the comparison report.

    report.py           the 4-model comparison tables
    report_document.py  the written report; holds CONTENDERS / ELIMINATED, the
                        single place the listening verdict lives in code
    audio_samples.py    picks and encodes the embedded audio examples
"""
